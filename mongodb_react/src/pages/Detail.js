import React from 'react';
import DetailContainer from '../containers/post/DetailContainer';

function Detail({profile}) {
    return (
        <DetailContainer profile={profile} />
    );
}

export default Detail;
